homepage:
    path:     /
    defaults: { _controller: SeoBundle:Default:index }

photo:
    path:     /photos
    defaults: { _controller: SeoBundle:Media:pictureForm }

add:
    path:     /photos/add
    defaults: { _controller: SeoBundle:Media:add }
