{% extends "base.html.twig" %}
{% block body %}
<div class="add_page">
	<div class="container">
		{{ form_start(form) }}
		<div class="row col-sm-12">
			{{ form_row(form.titre) }}
		</div>
		<div class="row col-sm-12">
			{{ form_row(form.contenu) }}
		</div>
		<div class="row col-sm-12">
			{{ form_row(form.date ) }}
		</div>
		
		{{ form_end(form) }}
	</div>
	
	{% endblock %}